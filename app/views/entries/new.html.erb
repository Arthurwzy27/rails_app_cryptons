<div class="forms">
  <div class="main-content">
    <div class="container py-5">
    <%= link_to '←', portfolio_path(@portfolio), class: 'btn btn-link', style: "display: left" %>
      <div class="row">
        <div class="col-12 col-md-6 offset-lg-3">
            <h1>Add a new entry</h1>
            <%= simple_form_for [@portfolio, @entry] do |f| %>
              <%= f.association :coin, prompt: 'Select a cryptocurrency', input_html: { data: { controller: 'select2' } } %>
              <%= f.input :amount, placeholder: 'Enter the amount you want!', collection: 0..100 %>
              <%= f.input :buying_price, placeholder: 'How much did you buy it?' %>
              <%= f.submit class: 'btn btn-flat' %>
            <% end %>
          </div>
      </div>
      <div class="coins-list d-flex">
            <div class="coins1-card">
            </div>
            <div class="coins2-card">
            </div>
            <div class="coins3-card">
            </div>
      </div>
    </div>
  </div>
</div>
